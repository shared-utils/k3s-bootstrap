# 节点数量：1 = 单节点，3 = 高可用（推荐奇数：1, 3, 5）
replicas: 1

storage:
  size: 5Gi
  # storageClassName: ""  # 使用默认 StorageClass

cluster:
  domain: cluster.local

# 资源限制（可选）
# resources:
#   requests:
#     memory: "256Mi"
#     cpu: "100m"
#   limits:
#     memory: "512Mi"
#     cpu: "500m"
